sap.ui.define([],
function() {
    "use strict";

    var constants = {
        BASE_URL: "http://localhost:8083",
        USERS_PATH: "/users",
        BOOKINGS_PATH: "/bookings"
    };

    return constants;
});